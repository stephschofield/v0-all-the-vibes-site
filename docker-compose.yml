# =============================================================================
# Local container run for the All The Vibes site (static Next.js app).
# The app currently requires no environment variables or backing services.
# =============================================================================

services:
  app:
    build:
      context: .
    ports:
      - "3000:3000"
